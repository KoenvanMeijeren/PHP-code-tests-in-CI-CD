{
    "name": "koenvanmeijeren/php-code-tests-in-ci-cd",
    "license": "private",
    "authors": [
        {
            "name": "Koen van Meijeren",
            "role": "developer"
        }
    ],
    "autoload": {
        "psr-4": {
            "Src\\": "src/"
        }
    },
    "require": {
        "php": "^8.1"
    },
    "require-dev": {
        "povils/phpmnd": "^3.0",
        "roave/security-advisories": "dev-master"
    },
    "scripts": {
        "phpmnd": [
            "phpmnd src/ --progress --extensions=default_parameter,-return,argument,array,assign,operation,property"
        ],
        "start": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up -d",
        "stop": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml stop"
    },
    "config": {
        "sort-packages": true
    }
}
